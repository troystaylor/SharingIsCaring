{
  "properties": {
    "connectionParameters": {
      "useBeta": {
        "type": "bool",
        "uiDefinition": {
          "displayName": "Use Microsoft Graph beta",
          "description": "If enabled, routes calls to /beta instead of /v1.0",
          "tooltip": "Enable to use Graph beta endpoints",
          "constraints": { "required": false }
        }
      }
    },
    "iconBrandColor": "#da3b01",
    "capabilities": ["actions"],
    "publisher": "Troy Taylor",
    "stackOwner": "Microsoft",
    "scriptOperations": [
      "InvokeMCP"
    ]
  }
}
