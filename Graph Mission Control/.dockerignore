# Build output is redirected outside the repo by Directory.Build.props, but these
# are excluded anyway so a stale local build never lands in the image.
**/bin/
**/obj/
**/.vs/
**/.vscode/
**/*.user
.azure/
appsettings.Development.json
