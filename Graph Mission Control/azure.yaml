# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: graph-mission-control
metadata:
  template: graph-mission-control@1.0.0

services:
  mcp:
    project: ./Graph Mission Control MCP
    host: containerapp
    language: dotnet
    docker:
      path: ./Dockerfile
      # Parent folder, so the linked capability index is inside the build context.
      context: ../
