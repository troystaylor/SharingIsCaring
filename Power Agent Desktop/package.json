{
  "name": "power-agent-desktop",
  "version": "1.0.0",
  "type": "module",
  "main": "src/dist/mcp-server/index.js",
  "scripts": {
    "install": "cd src && npm install",
    "build": "cd src && npm run build",
    "build:desktop": "cd src && npm run build:desktop",
    "build:all": "cd src && npm run build:all",
    "start": "cd src && npm run start",
    "start:desktop": "cd src && npm run start:desktop",
    "build:installer": "cd src && npm run build:installer"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.1.0"
  }
}
