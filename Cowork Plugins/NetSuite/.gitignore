# Build and deployment artifacts
NetSuite.zip
NetSuite-v*.zip
netsuite-cowork-plugin.zip
IMPLEMENTATION-PLAN.md

# azd local state
.azure/

# azd CLI logs (may contain subscription ids, resource names, FQDNs)
.azd-*.log

# azd / az deployment artifacts (contain subscription ids, principal ids, tenant ids)
nsuite.json
infra/preflight-*.bicepparam
infra/preflight-*.snapshot.json

# Generated icons (rebuild via generate-icons.ps1 / fix-outline.ps1)
color.png
outline.png

# VS Code/IDE
.vscode/
*.user

# OS files
.DS_Store
Thumbs.db

# Local config/secrets
server/appsettings.json
*.env
*.local.settings.json

# Build output
server/bin/
server/obj/
