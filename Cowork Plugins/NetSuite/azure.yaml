# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: netsuite-cowork-mcp
metadata:
  template: netsuite-cowork-mcp@0.1.0
services:
  mcp:
    project: ./server
    language: dotnet
    host: containerapp
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true
infra:
  provider: bicep
  path: infra
  module: main
