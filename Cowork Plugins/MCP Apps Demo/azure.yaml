name: mcp-apps-demo
metadata:
  template: mcp-apps-demo
services:
  server:
    project: ./server
    host: containerapp
    language: ts
    docker:
      path: ./Dockerfile
      context: .
      remoteBuild: true
