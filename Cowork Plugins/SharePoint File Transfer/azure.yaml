# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: sharepoint-file-transfer-mcp
metadata:
  template: sharepoint-file-transfer-mcp@0.2.0
services:
  mcp:
    project: ./server
    language: dotnet
    host: containerapp
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true
infra:
  provider: bicep
  path: infra
  module: main
