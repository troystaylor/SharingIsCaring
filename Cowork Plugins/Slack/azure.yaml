# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json
name: slack-cowork-mcp
metadata:
  template: slack-cowork-mcp@1.0.0
services:
  mcp:
    project: ./server
    language: dotnet
    host: containerapp
    docker:
      path: Dockerfile
      context: .
      remoteBuild: true
infra:
  provider: bicep
  path: infra
  module: main
