name: servicenow-slack-copilot-connector
metadata:
  template: azd-init
services:
  api:
    project: .
    language: ts
    host: function
    hooks:
      prepackage:
        shell: pwsh
        run: npm run build
