{
    "name": "copilot-studio-agent-middleware",
    "version": "2.0.0",
    "type": "module",
    "description": "Azure Function proxy that connects Salesforce to Copilot Studio agents via the M365 Agents SDK (Direct Connect).",
    "main": "src/functions/*.js",
    "engines": {
        "node": ">=18.0.0"
    },
    "scripts": {
        "start": "func start",
        "test": "echo \"No tests configured\" && exit 0"
    },
    "dependencies": {
        "@azure/functions": "^4.5.0",
        "@azure/msal-node": "^2.16.3",
        "@microsoft/agents-copilotstudio-client": "^1.2.3"
    }
}
