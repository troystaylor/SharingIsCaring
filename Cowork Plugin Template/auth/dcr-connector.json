{
    "_comment": "Dynamic Client Registration (DCR) — Use when your MCP server supports RFC 7591 dynamic client registration. Cowork automatically creates an OAuth client on your plugin's behalf. No authorization block needed.",
    "agentConnectors": [
        {
            "id": "{{service-name}}-api",
            "displayName": "{{Service Name}} API",
            "description": "{{What data and actions this connector provides}}",
            "toolSource": {
                "remoteMcpServer": {
                    "mcpServerUrl": "https://{{your-mcp-endpoint}}/mcp"
                }
            }
        }
    ]
}
