{
    "_comment": "OAuth 2.0 — Use for APIs that support standard OAuth 2.0 authorization code flow (recommended for production)",
    "agentConnectors": [
        {
            "id": "{{service-name}}-api",
            "displayName": "{{Service Name}} API",
            "description": "{{What data and actions this connector provides}}",
            "toolSource": {
                "remoteMcpServer": {
                    "mcpServerUrl": "https://{{your-mcp-endpoint}}/mcp",
                    "authorization": {
                        "type": "OAuthPluginVault",
                        "referenceId": "{{service-name}}-oauth"
                    }
                }
            }
        }
    ]
}
