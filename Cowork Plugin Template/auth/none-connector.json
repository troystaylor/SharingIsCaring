{
    "_comment": "No Auth — Use for public APIs, anonymous endpoints, or internal services behind network-level security",
    "agentConnectors": [
        {
            "id": "{{service-name}}-api",
            "displayName": "{{Service Name}} API",
            "description": "{{What data and actions this connector provides}}",
            "toolSource": {
                "remoteMcpServer": {
                    "mcpServerUrl": "https://{{your-mcp-endpoint}}/mcp",
                    "authorization": {
                        "type": "None"
                    }
                }
            }
        }
    ]
}
