# XML API
This custom code converts JSON requests to XML for APIs that require XML format, and converts XML responses back to JSON. Originally created for the Moneris payment gateway API.

# Getting Started
Define your action(s) with a JSON body for the request and response. Enable the custom code, making sure that you have enabled it for the Operation IDs needed.